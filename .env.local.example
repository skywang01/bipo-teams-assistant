# ── 接入真实 bipo-ai-service（Service Key 模式）─────────────────────────
# 复制为 .env.local（已 gitignore）并填入真实值。可直接复用 TNA-POC 的同名配置。

# 1. Vite 代理目标 —— 真实 service 基址（仅服务端可见）。
BIPO_TARGET=https://bipo-ai-test.bipocloud.com

# 2. Service Key（机器对机器）。由 Vite 代理注入 x-service-key 头（仅服务端，绝不进浏览器）。
#    在 Admin UI > Management > Service Keys 创建，scope 到目标 agent。
BIPO_SERVICE_KEY=sk_xxx

# （备选）IDP Bearer token —— 仅当 BIPO_SERVICE_KEY 为空时使用。
# BIPO_TOKEN=

# 3. 前端引擎："mock"（离线脚本 demo） | "real"（调 bipo-ai-service）
VITE_AGENT_MODE=real

# 4. 要调用的 agent id。
VITE_BIPO_AGENT_ID=attendance-ai

# ── 浏览器直连后端模式(需后端已开 CORS 放行本域名)──────────────────────
# 不用同源代理/BFF 时启用。设了下面两项 = 浏览器直接调绝对 URL 并自带 x-service-key。
# ⚠️ VITE_ 前缀的值会被打进浏览器 bundle(任何人可读)→ 仅 POC;生产须换 OAuth 并轮换 key。
# 留空(默认)则走同源 + 代理注入,key 不进浏览器。
# VITE_BIPO_BASE_URL=https://bipo-ai-test.bipocloud.com
# VITE_BIPO_SERVICE_KEY=sk_xxx

# 5. （可选）Teams SSO 用的 Entra 应用 client id（阶段2 接入，脚手架已预留）。
# VITE_TEAMS_CLIENT_ID=
